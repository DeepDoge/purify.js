import {
    Builder,
    Signal,
    awaited,
    computed,
    css,
    fragment,
    ref,
    sheet,
    tags,
} from "purify-js"
console.log(Builder, Signal, awaited, computed, css, fragment, ref, sheet, tags)
