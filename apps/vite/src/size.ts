import {
    Builder,
    Signal,
    awaited,
    computed,
    effect,
    fragment,
    ref,
    tags,
    toAppendable,
} from "purified-js"
console.log(Builder, Signal, awaited, computed, fragment, ref, tags, toAppendable, effect)
