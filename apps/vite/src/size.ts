import {
    Builder,
    Signal,
    awaited,
    computed,
    css,
    fragment,
    ref,
    sheet,
    tags,
} from "purified-js"
console.log(Builder, Signal, awaited, computed, css, fragment, ref, sheet, tags)
