import {
    awaited,
    Builder,
    computed,
    fragment,
    ref,
    Signal,
    tags,
    toAppendable,
} from "@purifyjs/purify"
console.log(Builder, Signal, awaited, computed, fragment, ref, tags, toAppendable)
